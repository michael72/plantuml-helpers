version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:16.4.1
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json"}}
      - run:
          name: install dependencies
          command: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package.json"}}
          paths:
            - ./node_modules
      - run:
          name: test
          command: npm install && npm run vscode:prepublish && npm t && codecov -f coverage/*.json
      - run:
          name: upload to codecov
          command: bash <(curl -s https://codecov.io/bash)
